import { OfferPipePipe } from './offer-pipe-pipe';

describe('OfferPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferPipePipe();
    expect(pipe).toBeTruthy();
  });
});
